<h1> Test from ENTRIES </h1>
<%= @project %>
<ul>
<% @project.entries.each do |x| %>
	<li>

	<%= x.hours %>
	<%= x.minutes %>
	<%= link_to("Edit", edit_project_entry_path(@project, x)) %>
	</li>
</ul>
<% end %>

<%= link_to("Projects", projects_path) %>
<%= link_to("New Entry", new_project_entry_path(@project)) %>
